<% remote_css_form_for :group, @group, :url => {:action => :update, :client_id => @client.id, :id => @group.id} do |f|%>
	<% @action = 'update' %>
	<%= render :partial => 'form', :locals => {:f => f}%>

<% end %>
